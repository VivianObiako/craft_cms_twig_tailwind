<header class="pb-10 blocks blocks--transparent">
	<div class="{{ _globals.get('blocksMedium') }}">
		{% if entry.newsRelatedCat and entry.newsRelatedCat.collect().first() %}
			<p class="mb-2 text-center text-beta dark:text-beta-400">
				{{ entry.newsRelatedCat.collect().first() }}
			</p>
		{% endif %}

		{% if entry.type == "home" %}
			<div class="mb-4 text-4xl text-center">
				🙈 🙉 🙊
			</div>
		{% endif %}

		<h1 class="mb-4 text-3xl text-center font-bold leading-none tracking-tight text-gray-900 sm:text-4xl md:text-5xl lg:text-6xl dark:text-white">{{ entry.title }}</h1>

		{% if entry.teaser %}
			<div class="{{ _globals.get('richTextIntro') }} text-center text-gray-500 dark:text-gray-300 mt-6">
				{{ entry.teaser }}
			</div>
		{% endif %}
	</div>
</header>
